{
  "name": "my-dbms-app",
  "version": "1.0.0",
  "description": "Simple DBMS application with Turso and Cloudflare",
  "type": "module",
  "scripts": {
    "pages:deploy": "wrangler pages deploy"
  },
  "dependencies": {
    "@libsql/client": "^0.5.6"
  }
}
